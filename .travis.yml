language: python
python:
  - 3.9
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
install:
  - pip install -r united-consortium-backend/requirements.txt
script: pytest
